# -*- coding: utf-8 -*-

# PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
# https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

from ccxt.base.exchange import Exchange
from ccxt.abstract.talapp import ImplicitAPI
from ccxt.base.types import Any, Market, Strings, Ticker, Tickers
from typing import List


class talapp(Exchange, ImplicitAPI):

    def describe(self) -> Any:
        return self.deep_extend(super(talapp, self).describe(), {
            'id': 'talapp',
            'name': 'Talapp',
            'countries': ['IR'],
            'rateLimit': 1000,
            'version': 'v1',
            'certified': False,
            'pro': False,
            'has': {
                'CORS': None,
                'spot': False,
                'margin': False,
                'swap': False,
                'future': False,
                'option': False,
                'fetchMarkets': True,
                'fetchTicker': True,
                'fetchTickers': True,
                'otc': True,
            },
            'options': {
                'defaultType': 'otc',
            },
            'urls': {
                'api': {
                    'public': 'https://talapp.ir',
                },
                'www': 'https://talapp.ir',
                'doc': 'https://talapp.ir/wp-json/talapp/v1/gold-prices',
            },
            'api': {
                'public': {
                    'get': {
                        'wp-json/talapp/v1/gold-prices': 1,
                    },
                },
            },
        })

    def fetch_markets(self, params={}) -> List[Market]:
        response = self.publicGetWpJsonTalappV1GoldPrices(params)
        return [self.parse_market(response)]

    def parse_market(self, response) -> Market:
        stale = self.safe_bool(response, 'stale', False)
        return {
            'id': 'XAU18IRT',
            'symbol': 'XAU18/IRT',
            'base': 'XAU18',
            'quote': 'IRT',
            'settle': None,
            'baseId': 'XAU18',
            'quoteId': 'IRT',
            'settleId': None,
            'type': 'otc',
            'spot': False,
            'margin': False,
            'swap': False,
            'future': False,
            'option': False,
            'active': not stale,
            'contract': False,
            'linear': None,
            'inverse': None,
            'contractSize': None,
            'expiry': None,
            'expiryDatetime': None,
            'strike': None,
            'optionType': None,
            'precision': {
                'amount': None,
                'price': None,
            },
            'limits': {
                'leverage': {'min': None, 'max': None},
                'amount': {'min': None, 'max': None},
                'price': {'min': None, 'max': None},
                'cost': {'min': None, 'max': None},
            },
            'created': None,
            'info': response,
        }

    def fetch_ticker(self, symbol: str, params={}) -> Ticker:
        self.load_markets()
        market = self.market(symbol)
        response = self.publicGetWpJsonTalappV1GoldPrices(params)
        return self.parse_ticker(response, market)

    def fetch_tickers(self, symbols: Strings = None, params={}) -> Tickers:
        self.load_markets()
        if symbols is not None:
            symbols = self.market_symbols(symbols)
        response = self.publicGetWpJsonTalappV1GoldPrices(params)
        market = self.market('XAU18/IRT')
        ticker = self.parse_ticker(response, market)
        result = {}
        result[ticker['symbol']] = ticker
        return self.filter_by_array_tickers(result, 'symbol', symbols)

    def parse_ticker(self, response, market: Market = None) -> Ticker:
        fetchedAt = self.safe_string(response, 'fetched_at')
        timestamp = None
        if fetchedAt is not None:
            timestamp = self.parse8601(fetchedAt.replace(' ', 'T') + '+03:30')
        return self.safe_ticker({
            'symbol': market['symbol'],
            'timestamp': timestamp,
            'datetime': None,
            'high': None,
            'low': None,
            'bid': self.safe_number(response, 'buy_gold'),
            'bidVolume': None,
            'ask': self.safe_number(response, 'sell_gold'),
            'askVolume': None,
            'vwap': None,
            'open': None,
            'close': self.safe_number(response, 'sell_gold'),
            'last': self.safe_number(response, 'sell_gold'),
            'previousClose': None,
            'change': None,
            'percentage': self.safe_number(response, 'sell_diff'),
            'average': None,
            'baseVolume': None,
            'quoteVolume': None,
            'info': response,
        }, market)

    def sign(self, path, api='public', method='GET', params={}, headers=None, body=None):
        url = self.urls['api'][api] + '/' + self.implode_params(path, params)
        query = self.omit(params, self.extract_params(path))
        if query:
            url += '?' + self.urlencode(query)
        headers = {'Accept': 'application/json'}
        return {'url': url, 'method': method, 'body': body, 'headers': headers}
